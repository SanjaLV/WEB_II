function MakeBet(loot_id) {
        str = "/auction/bet/" + loot_id;
        window.location = str;
}

function BuyItem(loot_id) {
        str = "/auction/buy/" + loot_id;
        window.location = str;
}